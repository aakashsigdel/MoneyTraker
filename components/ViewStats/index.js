'use strict';

import React, {
  Component,
  View,
} from 'react-native';
import MoneyDetails from '../MoneyDetails';

export default class ViewStats extends Component {
  render () {
    return (
      <MoneyDetails />
    );
  }
}
